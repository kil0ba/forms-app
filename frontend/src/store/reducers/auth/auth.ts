import {AuthAction, AuthInitialState} from "./auth.model";

const authReducer = (state: AuthInitialState = new AuthInitialState(), action: AuthAction) => {
  return state;
};

export default authReducer;
